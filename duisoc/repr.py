   def process_elements(elements):
       if len(elements) < 3:
           # Handle the edge case with less than 3 elements
           # ...
       else:
           # Proceed with the regular code path for 3 or more elements
           # ...
   